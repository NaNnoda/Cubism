{
  "dependencies": {
    "@mdi/svg": "^7.0.96",
    "esbuild": "^0.15.9"
  },
  "scripts": {
    "build": "esbuild src/Index.ts --bundle --outfile=build.js",
    "watch": "esbuild src/Index.ts --bundle --sourcemap --outfile=build.js --watch --format=esm --global-name=cubism"
  },
  "devDependencies": {
    "@types/node": "^18.8.2"
  }
}
